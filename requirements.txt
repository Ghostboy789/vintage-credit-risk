# Requirements file -- append new lines, don't reorder existing ones.
# extract/
pandas==2.3.3  # lifelines requires pandas<3
pyarrow==25.0.1
duckdb==1.5.5

# dev / quality
pytest==9.1.1
ruff==0.16.8

# docs/scripts (reading the Freddie Mac file layout spreadsheet)
openpyxl==3.1.5

# fixtures (scripts/make_fixtures.py)
numpy==2.4.6

# PD engine (models/pd/)
optbinning==1.0.0
statsmodels==0.15.0
scikit-learn==1.9.1
lightgbm==4.7.0
scipy==1.17.1

# dbt on BigQuery (dbt/), CI runs the "ci" target on dbt-duckdb over synthetic fixtures
dbt-core==1.12.5
dbt-bigquery==1.12.1
dbt-duckdb==1.11.0
dbt-metricflow==0.15.0
metricflow==0.213.0
google-cloud-bigquery==3.45.2  # scripts/export_marts.py's --target bq path only
google-cloud-bigquery-storage==2.41.0  # fast paged Arrow streaming for the same path
